## 为什么 /health 路由要单独存在,它和 /api/trip/plan 有什么本质区别?

## 什么是schema?



L3: 调通LLM中容易踩的坑

| 坑                                                           | 怎么避                                                       |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| LLM 输出包含 markdown 代码块(````json`)而不是纯 JSON         | 在 system prompt 里强调"**只输出合法 JSON,不要任何解释、不要 markdown 包裹**",或在解析前手动 strip 掉 |
| LLM 返回了"键名拼错"或"必填字段缺失",`model_validate_json` 抛 ValidationError | 这是 L8 才学的"失败重试",L3 阶段**直接报错是 OK 的**,你能看到错误就说明校验生效了 |
| `LLM_BASE_URL` 末尾带不带 `/v1` 不同厂商不同                 | 你用的服务商文档为准;国内厂商多数带 `/v1`                    |
| `.env` 没生效                                                | `from dotenv import load_dotenv; load_dotenv()`,在 main.py 启动时执行一次 |

`app/api/` 下两个文件的分工应该清楚:

| 文件             | 角色                                        | 关键变量               |
| ---------------- | ------------------------------------------- | ---------------------- |
| `main.py`        | FastAPI **应用入口**,负责启动和注册所有路由 | `app = FastAPI()`      |
| `routes/trip.py` | 单个业务模块的**路由集合**                  | `router = APIRouter()` |



## LLM回答清洗：

LLM不是API，它的输出格式不可预测，工程上必须兜底。当LLM是**"推理模型"(reasoning model)**,比如 DeepSeek-R1、Qwen-Reasoning、QwQ之类。这类模型在给出最终答案前会先做"内心独白"(`<think>` 块),再输出 JSON。模型能力越强,这个块越长。

解决方法：**LLM 输出污染的形式其实有好几种**:

| 污染类型             | 例子                      |
| -------------------- | ------------------------- |
| `<think>` 块(有标签) | `<think>...{...}...{...}` |
| 裸思考(无标签)       | `The user wants...{...}`  |
| markdown 包裹        | ````json\n{...}\n````     |
| 前缀说明             | `下面是行程:\n{...}`      |
| 后缀解释             | `{...}\n希望你喜欢`       |

**靠逐个匹配标签永远追不完**,需要更底层的兜底。



解决方法：

按"成本从低到高、效果从大到小"排列:

```
源头治理 → Prompt 约束 → 末端清洗 → 重试循环
 ↑最优 ↓兜底
```

### 第1 层:源头治理(治本,优先做)

**核心思路:让污染不发生,而不是发生后再清洗。**

具体手段,按推荐度排:

| 手段                       | 怎么做                                                       | 效果                                   |
| -------------------------- | ------------------------------------------------------------ | -------------------------------------- |
| **关掉 thinking**          | 查 MiniMax-M3 API 文档,看 `extra_body={"thinking": {"type": "disabled"}}` 或类似参数 | ⭐⭐⭐⭐⭐ 直接从源头消除 90% 污染          |
| **换非 thinking 模型**     | 查服务商有没有 `MiniMax-M3-fast` / `-instruct` / `-non-thinking` 后缀的版本 | ⭐⭐⭐⭐⭐ 同上,且响应更快更便宜            |
| **降低 temperature**       | 改成 `temperature=0.1` 甚至 `0.0`,让输出更确定               | ⭐⭐⭐ 减少随机性,但不解决 thinking       |
| **使用 structured output** | OpenAI 有 `response_format={"type": "json_object"}`,Anthropic 有 tool_use | ⭐⭐⭐⭐ API 层强约束,但不是所有模型都支持 |

**对你这个项目,第一件事就应该是查 MiniMax-M3 能不能关 thinking**。这是治本。如果可以,所有清洗逻辑都可以删掉。

### 第 2 层:Prompt 约束(治标,辅助)

在 system prompt 里**反复、明确**告诉 LLM 只输出 JSON:

```
输出要求(必须严格遵守):
1. 输出必须是合法 JSON,不要任何前置说明2. 不要用 markdown 代码块包裹
3. 不要输出<think>
5. JSON 必须完整,从 { 开始到 } 结束,中间没有其他文字
```

这个方法**对非 thinking 模型有效,对 thinking 模型基本无效**——因为 thinking 是模型自己产生的,不是 prompt 能控制的。

### 第 3 层:末端清洗(兜底,必须做)

你已经学会了:**用括号配对算法找最外层 `{...}`**,这是结构性的不变量,跟模型输出无关。

工程上要做到:

1. **清洗逻辑独立成函数**(`extract_json`),方便单测
2. **多层兜底**:即便括号配对成功,再用 `json.loads()` 验证一次合法性
3. **失败时记录原始响应**(打到日志),便于事后分析、prompt 调优

### 第 4 层:重试循环(最末,救命用)

如果清洗失败、JSON 还是解析不了,**不要直接给用户报错**,而是:

```
LLM 输出 → 清洗 → JSON 解析
 ↓ 失败
                   带错误反馈,重新调用 LLM(最多 N 次)
                       ↓ 实在不行,降级返回最简结果
```



## LLM偷懒，编造景点，饭店，，，要解决所有事实都要从外部拿



## 把"极端低预算"case 拆出来标"压力测试"

## 优化 prompt 让 meal_in_candidates 提到 85%+



加入缓存机制，避免搞得API的重复调用



边界问题：

- 高德的天气预测只覆盖4天，长程规划涉及边界问题
